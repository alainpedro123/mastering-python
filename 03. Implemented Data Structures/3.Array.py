# 17. ARRAYS - similar like List but they're useful ONLY for a large sequence of numbers, They take less memory and perform faster
from array import array

numbers = array("i", [1, 2, 3])
print(numbers)
# methods: append, insert, pop, remove... etc
